#include<bits/stdc++.h>
using namespace std;
#define has(s,key) s.find(key)!=s.end()

int main(void) {
	int t;
	cin>>t;
	int n;
	while(t--) {
		cint>>n;
		vector<int> ar(n);
		for(int i=0;i<n;i++)
			cin>>ar[i];
	}
	return 0;
}